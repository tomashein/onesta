import Layout from '@/layout';
import type { NextPage } from 'next';

const Home: NextPage = () => {
  return <Layout title="Dashboard">There should be a cool dashboard here!</Layout>;
};

export default Home;

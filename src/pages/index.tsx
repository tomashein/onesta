import type { NextPage } from 'next';
import Head from 'next/head';

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Onesta</title>
        <meta name="description" content="Test app development with NextJS and Tailwind CSS" />
      </Head>
    </div>
  );
};

export default Home;
